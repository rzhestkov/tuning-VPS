# VPS Setup Scripts

Автоматическая настройка свежих VPS-серверов с защитой от блокировки. 
Скрипты настраивают SSH, файрвол, Docker и автоматические обновления.

## ⚡ Быстрый старт (Ubuntu 24.04)

```bash
# 1. Подключитесь к серверу как root
ssh root@YOUR_SERVER_IP

# 2. Скачайте скрипт
curl -fsSL https://raw.githubusercontent.com/rzhestkov/tuning-VPS/main/setup_ubuntu_24.04.sh -o setup.sh

# 3. Запустите
sudo bash setup.sh
```

## 📁 Обновление проекта в репозиториях

git push origin main   # в Gitea
git push github main   # в GitHub

## 📁 Структура проекта

```
vps-setup/
├── README.md                      # Этот файл
├── setup_ubuntu_24.04.sh          # Скрипт для Ubuntu 24.04 LTS
├── setup_debian_13.sh             # (в разработке) Debian 13
└── ssh/
    └── authorized_keys            # Ваши публичнын SSH ключи (id_rsa.pub)
```

## 🔑 Подготовка перед первым запуском

### 1. Поместите SSH ключ в репозиторий

В папке ssh должен лежать готовый файл authorized_keys

## 2. Настройте переменные в скрипте

Убедитесь, что в `setup_ubuntu_24.04.sh` верно прописаны:

```bash
GITHUB_USER="YOUR_GITHUB_USERNAME"  # Ваш аккаунт на GitHub
REPO_NAME="vps-setup"               # Название этого репозитория
NEW_USER="user1"                    # Имя нового пользователя (можно оставить)
SSH_PORT="2332"                     # Порт для SSH (можно изменить)
```

## 🚀 Детальная инструкция

### Шаг 1: Первое подключение

Подключитесь к свежему VPS через веб-консоль хостинга или SSH как `root` с временным паролем.

### Шаг 2: Запуск скрипта

```bash
# Скачивание
curl -fsSL https://raw.githubusercontent.com/USERNAME/REPO/main/setup_ubuntu_24.04.sh -o setup.sh

# Просмотр (опционально, но рекомендуется)
cat setup.sh | less

# Запуск
chmod +x setup.sh
sudo ./setup.sh
```

### Шаг 3: Контрольная проверка (ВАЖНО!)

**Не закрывайте текущее окно терминала!**

Откройте **новое окно** на вашем компьютере и проверьте подключение:

```bash
ssh -p 2332 user1@SERVER_IP
```

Если подключение работает — вернитесь в первое окно и нажмите `yes` для закрытия старого порта 22.

## 🛡️ Что делает скрипт

| Этап                   | Описание                              | Защита от ошибок                                                 |
| ---------------------- | ------------------------------------- | ---------------------------------------------------------------- |
| **Проверки**           | Проверка root, Ubuntu, GitHub         | Останавливает если не root или нет связи с GitHub               |
| **Обновление**         | `apt update && upgrade` + mc          | Проверка подключения к сети                                      |
| **Временная зона**     | Устанавливает Europe/Berlin           | Автоматическая настройка через timedatectl                       |
| **NTP синхронизация**  | Проверяет и настраивает время         | Проверка systemd-timesyncd, принудительная синхронизация         |
| **Journald**           | Настраивает системный журнал          | Persistent storage, лимиты 500МБ, сжатие, срок хранения 1 месяц  |
| **Logrotate**          | Настраивает ротацию логов            | Ежедневная ротация, 7 копий, сжатие для системных логов          |
| **Пользователь**       | Создаёт `user1`, добавляет в sudo     | Проверка существования - пропускает если уже есть               |
| **SSH ключи**          | Качает ключ с GitHub                  | Бэкап существующих ключей + предупреждение о замене             |
| **SSH настройка**      | Меняет порт на 2332, отключает пароли | Бэкап конфигов, `sshd -t`, не закрывает 22 сразу                 |
| **Cloud-init**         | Отключает переопределение настроек    | Исправляет `50-cloud-init.conf` (проблема VDSina)                |
| **Socket→Service**     | Переключает SSH с socket на service   | Предотвращает сброс настроек при ребуте                          |
| **Firewall**           | UFW: разрешает 2332, 80, 443          | Сначала открывает новый порт, потом включает файрвол             |
| **Fail2ban**           | Защита SSH от брутфорса               | 3 попытки, бан на 1 час, настроен на кастомный порт SSH          |
| **Автообновления**     | Настраивает `unattended-upgrades`     | Только security-обновления, без перезагрузки сервисов            |
| **Hardening ядра**     | Настройка `/etc/sysctl.conf`          | Защита от SYN-flood, отключение IP forwarding, kernel.dmesg_restrict |
| **Ограничения ресурсов** | Настройка `/etc/security/limits.conf` | Защита от fork-bomb, ограничение процессов и файлов              |
| **MOTD**               | Кастомное сообщение при входе         | Убрана информация о версии ОС, только безопасное сообщение       |
| **Отключение сервисов**| Отключение ненужных сервисов          | snapd, apport, whoopsie, lxd, bolt, udisks2, ModemManager, bluetooth |
| **Закрытие 22**        | Удаляет правило для стандартного SSH  | **Только после вашего подтверждения**, проверяет если уже закрыт |
| **Диагностика**        | Отчёт о настройках + проверки         | Статусы: порт, SSH, UFW, вход по паролю откл., fail2ban         |
| **Проверка пакетов**   | Показывает предустановленные пакеты   | Python, Docker, Nginx, MySQL, Redis и др. + место на диске       |
| **Docker**             | Устанавливает Docker CE (опционально) | Пропускает если уже установлен                                   |

### Проверки при повторном запуске

Скрипт оптимизирован для повторного запуска:
- **Пользователь** — если `user1` уже существует, создание пропускается
- **SSH ключи** — если `authorized_keys` уже есть, создаётся бэкап и выводится предупреждение
- **UFW правила** — проверяет существование правил перед добавлением
- **Порт 22** — проверяет, закрыт ли порт, и не задаёт вопрос если уже закрыт
- **Docker** — пропускает установку если уже есть

## ⚠️ Безопасность и защита от блокировки

### Почему вы не потеряете доступ:

1. **Двойное окно**: Скрипт просит подтвердить закрытие порта 22 только после ручной проверки подключения на новом порту
2. **Бэкапы**: Все конфиги SSH сохраняются с timestamp (`sshd_config.bak.1234567890`)
3. **Проверка синтаксиса**: Перед перезапуском SSH проверяется `sshd -t`
4. **Cloud-init fix**: Специально обрабатывает `50-cloud-init.conf`, который на некоторых хостингах (VDSina, Selectel) сбрасывает настройки SSH

### Если всё же потеряли доступ:

Через веб-консоль хостинга (VNC):

```bash
# Восстановить SSH конфиг
sudo cp /etc/ssh/sshd_config.bak.* /etc/ssh/sshd_config
sudo systemctl restart ssh

# Или отключить файрвол
sudo ufw disable
```

## 🔍 Диагностика после установки

Скрипт выводит отчёт в конце, но вы можете проверить вручную:

```bash
# Проверка SSH
sudo systemctl status ssh
sudo ss -tlnp | grep ssh

# Проверка файрвола
sudo ufw status verbose

# Проверка Docker
docker --version
sudo -u user1 docker ps

# Проверка автоматических обновлений
systemctl list-timers apt-daily-upgrade.timer
cat /var/log/unattended-upgrades/unattended-upgrades.log | tail -5

# Проверка временной зоны и времени
timedatectl
timedatectl status | grep "System clock synchronized"

# Проверка systemd-timesyncd
sudo systemctl status systemd-timesyncd

# Проверка journald (системный журнал)
sudo journalctl --disk-usage
sudo systemctl status systemd-journald

# Проверка logrotate
sudo logrotate -d /etc/logrotate.d/custom-system

# Проверка fail2ban
sudo systemctl status fail2ban
sudo fail2ban-client status sshd

# Проверка предустановленных пакетов (что делает скрипт автоматически)
python3 --version
docker --version
nginx -v
mysql --version
redis-server --version
```

## 🐛 Troubleshooting

### "Настройки SSH сбрасываются после ребута"

**Причина**: Cloud-init перезаписывает конфиг.
**Решение**: Скрипт автоматически отключает `50-cloud-init.conf`, но если проблема осталась:

```bash
sudo rm /etc/ssh/sshd_config.d/50-cloud-init.conf
sudo systemctl restart ssh
```

### "UFW блокирует соединение"

```bash
# Временно отключить
sudo ufw disable

# Или добавить свой IP вручную (замените YOUR_IP)
sudo ufw allow from YOUR_IP to any port 2332
```

### "Docker не доступен пользователю"

```bash
# Перелогиниться или выполнить:
sudo usermod -aG docker $USER
newgrp docker
```

## ➕ Добавление новой ОС

Если хотите добавить поддержку другой системы (например, Debian 12):

1. Скопируйте `setup_ubuntu_24.04.sh` в `setup_debian_12.sh`
2. Измените:
   - Пакетный менеджер (если нужно)
   - Имена сервисов (`ssh` vs `sshd`)
   - Пути к конфигам
3. Протестируйте на тестовом сервере
4. Обновите эту таблицу в README:

| ОС               | Файл                    | Статус          | Примечания                   |
| ---------------- | ----------------------- | --------------- | ---------------------------- |
| Ubuntu 24.04 LTS | `setup_ubuntu_24.04.sh` | ✅ Готов         | Проверено на VDSina, Timeweb |
| Debian 12        | `setup_debian_12.sh`    | 🚧 В разработке | -                            |
| AlmaLinux 9      | `setup_almalinux_9.sh`  | 📋 Планируется  | Требуется адаптация под dnf  |

## 📝 Лог изменений

- **v1.5** - Дополнительная настройка безопасности
  
  - **Hardening ядра**: Настройка sysctl.conf (защита от SYN-flood, отключение IP forwarding, kernel.dmesg_restrict)
  - **Ограничения ресурсов**: Настройка limits.conf (защита от fork-bomb, ограничение процессов и файлов)
  - **MOTD**: Кастомное сообщение при входе без информации о версии ОС
  - **Отключение сервисов**: snapd, apport, whoopsie, lxd, bolt, udisks2, ModemManager, bluetooth

- **v1.4** - Полная настройка безопасности и логирования
  
  - **Временная зона**: Установка Europe/Berlin через timedatectl
  - **NTP синхронизация**: Проверка и настройка systemd-timesyncd
  - **Journald**: Persistent storage, лимит 500МБ, сжатие, срок хранения 1 месяц
  - **Logrotate**: Ротация системных логов, 7 копий, ежедневно
  - **Fail2ban**: Защита SSH от брутфорса (3 попытки, бан на 1 час)
  - Обновлена нумерация секций скрипта (1-13)

- **v1.3** - Улучшенные проверки при повторном запуске
  
  - Проверка существования пользователя user1 (пропускает создание)
  - Бэкап authorized_keys с предупреждением о замене
  - Добавлен статус "Вход по паролю отключен" в отчёт

- **v1.2** - Безопасный повторный запуска
  
  - Проверка существования UFW правил (избегает дублей при повторном запуске)
  - Проверка закрытия порта 22 (пропускает вопрос если уже закрыт)
  - Автоматическая установка mc без вопросов

- **v1.1** - Проверка предустановленных пакетов
  
  - В конце скрипта выводится список уже установленных пакетов
  - Показывает свободное место на диске и использование памяти
  - Docker устанавливается только если не был предустановлен

- **v1.0** - Базовая версия для Ubuntu 24.04
  
  - Защита от блокировки при смене SSH порта
  - Автоматическая обработка cloud-init
  - Пошаговая диагностика

## ⚖️ Лицензия

MIT License - используйте на свой страх и риск. Всегда проверяйте скрипты перед запуском на продакшене!

---

**Важное напоминание**: Никогда не коммитьте приватные ключи (`id_rsa`) в Git! В репозиторий кладите только публичные ключи (`id_rsa.pub` → `authorized_keys`).

## 💡 Советы по использованию

1. **Сделайте репозиторий приватным**, если не хотите светить своим SSH ключом (хотя публичный ключ и безопасен, это просто хорошая практика)

2. **Для автоматизации** можно создать alias у себя на компьютере:
```bash
# Добавьте в ~/.bashrc или ~/.zshrc
alias setup-vps='curl -fsSL https://raw.githubusercontent.com/USERNAME/REPO/main/setup_ubuntu_24.04.sh | sudo bash'
```

3. **Тестирование**: Перед запуском на дорогом сервере, потратьте $1-2 на тестовый VPS у того же провайдера, чтобы проверить совместимость.
